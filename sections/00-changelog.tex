\section*{Changelog}
\addcontentsline{toc}{section}{Changelog}

\begin{description}
\item[2026-05-27] Version renamed from OSDP~2.3 to OSDP~3.0 throughout. Consolidated PIV Auto, biometric-result, VCI trust-anchor, and SPE updates:
\begin{itemize}
  \item Removed \texttt{osdp\_PIVSPE} flag \texttt{0x01} (retry continuation); each \texttt{osdp\_PIVSPE} command now initiates exactly one PIN-entry session and \texttt{osdp\_PIVSPER} terminates the session unconditionally, including on PIN mismatch. ACUs wishing to allow additional attempts \textbf{MAY} re-issue \texttt{osdp\_PIVSPE}, using the \emph{Attempts Remaining} count from the preceding \texttt{osdp\_PIVSPER} to guide retry policy. Bit \texttt{0x01} is now reserved.
  \item Aligned \texttt{osdp\_PIVSPER} and \texttt{osdp\_PIVBIOMATCHR} result codes for credential removal, changed \texttt{osdp\_PIVBIOMATCHR} \texttt{0x03} to PIN verification required, reserved \texttt{osdp\_PIVBIOMATCHR} \texttt{0x07}, clarified biometric assurance limits, and specified that only off-card biometric match success returns the 32-byte SHA-256 hash of tag \texttt{0xBC}.
  \item Clarified the Function Code~14 biometric-support reference for \texttt{osdp\_PIV\_LOADTPK} and added CAC as PIV mode data model \texttt{0x03}.
  \item Added PIV Auto KMAC challenge derivation, required series and sequence counters, fixed-width supplemental entropy, and the \texttt{osdp\_PIVSTATUSR} poll reply for reporting credential identity and signed challenge responses. PIV Auto is configured through \texttt{osdp\_PIVMODE} and is restricted to Secure Channel~2.0 sessions that are not using factory keys.
  \item Replaced the SP~800-73 Intermediate CVC anchor model with an OSDP-defined VCI trust anchor record (tag \texttt{0x7F50}) carrying a Profile Identifier (\texttt{0x80} = \texttt{0x01}), an eight-byte Issuer Identifier Number (\texttt{0x42}), and a DER-encoded SubjectPublicKeyInfo (\texttt{0x7F49}). Maximum conforming record size is 576~bytes (\texttt{0x0240}), derived from the RSA-4096 key case.
  \item Added normative IIN derivation rule: the IIN in \texttt{osdp\_PIV\_VCILOADTA} and in the trust anchor record \textbf{SHALL} be the leftmost eight bytes of the subjectKeyIdentifier from the content-signing X.509 certificate.
  \item Tightened \texttt{osdp\_PIV\_VCILOADTA} multi-part encoding: Anchor Length and MpSizeTotal ranges corrected to \texttt{0x0000}--\texttt{0x0240}; added normative constraint that \texttt{MpOffset}~+~\texttt{MpFragmentSize} \textbf{SHALL NOT} exceed \texttt{Anchor Length}; PD \textbf{SHALL} verify the reassembled byte string is exactly \emph{Anchor Length} bytes and a single \texttt{0x7F50} record before committing.
  \item Documented variable-length PD storage accounting: PDs \textbf{SHALL} account for anchors by their actual \emph{Anchor Length}, not a fixed maximum; replacement is atomic (prior anchor remains if replacement fails); delete releases actual stored bytes.
  \item Specified the two-tier PD verification path during VCI establishment: direct (trust anchor IIN matches secure messaging CVC IIN, anchor SPKI verifies CVC signature) and intermediate (no direct match triggers retrieval of PIV data object \texttt{0x1017}/\texttt{5FC122}; Intermediate CVC Subject Identifier must equal CVC IIN, Role \texttt{0x5F4C} must be \texttt{0x12}, anchor IIN must match Intermediate CVC IIN, anchor SPKI verifies Intermediate CVC, Intermediate CVC verifies secure messaging CVC). PDs \textbf{SHALL NOT} perform X.509 path validation or revocation for VCI trust anchors.
  \item Added normative ACU post-VCI validation requirements: after \texttt{osdp\_CARDSTATUSR} reports VCI established, the ACU \textbf{SHALL} obtain the content-signing certificate (from cache or via \texttt{osdp\_PIVDATA} for \texttt{0x1017}/inner tag \texttt{0x70}), verify SKI matches \emph{VCI Anchor IIN}, verify SPKI matches the stored anchor record, verify PIV or PIV-I content-signing EKU OID, verify validity period (expired certificate \textbf{SHALL} cause session rejection and anchor unload), and perform X.509 path validation and revocation per deployment policy.
  \item Restored \emph{VCI Trust Anchor ID} to \texttt{osdp\_CARDSTATUSR} with normative language; added \emph{VCI CVC IIN} and \emph{VCI Anchor IIN} fields (each 8~bytes) so the ACU receives the IIN pair needed to distinguish direct and intermediate verification paths.
  \item Updated \texttt{osdp\_PIV\_VCITASTATUSR}: clarified that \emph{Available Bytes} reflects variable-length accounting and that the per-record IIN is the anchor selector from the stored trust anchor record, not necessarily the secure messaging CVC IIN reported in \texttt{osdp\_CARDSTATUSR}.
  \item Added VCI security preconditions: all VCI-related commands \textbf{SHALL} be rejected unless OSDP Secure Channel~2.0 is active and not using factory/default keys; failures reported via \texttt{osdp\_PDERROR} \texttt{0x0006}.
\end{itemize}
\item[2026-01-21] Error-handling cleanup and code assignments: \textbf{(1)} \texttt{osdp\_PIVPUTDATA} now \textbf{SHALL} report failures via \texttt{osdp\_PDERROR} (status word optional) so writes can’t silently succeed. \textbf{(2)} \texttt{osdp\_PIVSPE} adds result code \texttt{0x09} in \texttt{osdp\_PIVSPER} for “VCI not established.” \textbf{(3)} PDERROR is credential/global only; PD capability gaps stay \texttt{osdp\_NAK}. \textbf{(4)} Legacy 2.2 replies (e.g., \texttt{osdp\_CRAUTHR}, \texttt{osdp\_PIVDATAR}) remain unchanged, with optional follow-up \texttt{osdp\_PDERROR} detail if the PD has it. \textbf{(5)} Documented tentative command/reply codes for the new PIV set: commands \texttt{0xA6} (VCI trust-anchor status) through \texttt{0xAF} (VCI trust-anchor load), and replies \texttt{0x85}--\texttt{0x88} for the new responses.
\item[2026-01-07] Added a PIV Mode interface-mask table aligned to Function Code 13 bits, clarified \texttt{osdp\_CRAUTHR} reply structure (full GENERAL AUTHENTICATE response without SW1\textbar SW2; status words reported via \texttt{osdp\_PDERROR} only), tightened \texttt{osdp\_CARDSTATUS}/\texttt{osdp\_CARDSTATUSR} behavior (solicited-only after PD preparation; error on credential removal), remapped the PIV error codes to the \texttt{0x10xx} block per subcommittee guidance, added PIV APDU references, aligned \texttt{osdp\_PIVDATA} tag selection to allow Discovery (\texttt{0x7E})/BIT (\texttt{0x7F61}) subtags in line with SP\,800-73-4, clarified that \texttt{osdp\_PDERROR} conveys a single error per reply, and clarified \texttt{osdp\_PIV\_XMITPAIRING} handling so pairing codes can be provided before VCI is established when the credential requires them.
\item[2025-12-10] Consolidated application-level errors on \texttt{osdp\_PDERROR} (two-byte codes, optional SW1\textbar SW2 for APDU failures, multi-error support); moved the PIV error set to \texttt{0x0100}--\texttt{0x01FF} and updated PIV commands to use \texttt{osdp\_PDERROR} (transport faults remain \texttt{osdp\_NAK}); made the Global Interface Mask in \texttt{osdp\_PIVMODE} a required byte (no TLV tag) with an updated field layout; clarified that \texttt{osdp\_CARDSTATUSR} is solicited-only after PD preparation with legacy poll-based \texttt{osdp\_RAW} as the aggregated card-present indication; and noted the deprecation of \texttt{osdp\_GENAUTH} with a recommendation to migrate to \texttt{osdp\_CRAUTH}.
\item[2025-12-04] Clarified that \texttt{osdp\_CRAUTHR} returns the full \texttt{GENERAL AUTHENTICATE} data including the Dynamic Authentication Template (excluding only the ISO/IEC~7816 status word).
\item[2025-11-12] Added PIV Biometric Match request/reply and tables (one-bit type values only); moved Enhanced PIV NAKs to a 0x20+ base (added 0x27 Credential Not Present/Removed, 0x28 Biometric Type Not Supported; clarified 0x23 Credential Security Status Not Satisfied); aligned CRAUTHR error handling to reference the enhanced NAK set; relocated Enhanced PIV (0x04) and PIV Auto (0x08) indicators to Function Code 12 and cleaned Function Code 13 to interface-only bits (removed non-interface flags; removed PIV Auto from Function Code 14); updated CARDSTATUS gating to Function Code 12 bit 0x04; added a PIV Mode Flags table and clarified that any legacy AID lists cited were from prior proposals (not OSDP~2.2); enabled PDF TOC hyperlinks and improved table/caption pagination.
\item[2025-10-30] Clarified CRAUTH command/reply behaviour (including RSA, AES, and ECC support), overhauled PIV Mode configuration with TLV-based application and interface controls (including PIV Auto), added card status request/reply messages, expanded PIV data retrieval semantics, introduced PIV Put Data, PIV Secure PIN Entry, TWIC privacy-key load, VCI pairing transmit, and VCI trust-anchor load/status commands, and documented the enhanced error-code set (including VCI/PIV Auto conditions).
  \item[2025-09-17] Major PIV Mode and SPE revisions.
  \item[2025-06-25] Added multi-AID usage and VCI support notes.
  \item[2025-05-27] New function code definitions for 3.0 biometric handling.
\end{description}
