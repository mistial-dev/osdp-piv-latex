\section{Packet Format}

The packet layout uses a structured table generated from a reusable snippet. Update the rows in `tables/packet-format.tex`; no LaTeX styling changes are required.

\begin{PacketTable}{Table 1 -- Packet Format}
  \input{tables/packet-format.tex}
\end{PacketTable}

The accompanying notes appear in Appendix~\ref{app:notes}.
