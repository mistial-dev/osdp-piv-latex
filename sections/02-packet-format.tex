\section{Packet Format}

The packet layout uses a structured table generated from a CSV file. Update the CSV in `tables/packet-format.csv`; no LaTeX changes are required.

\PacketTableFromFile{tables/packet-format.tex}{Table 1 -- Packet Format}

The accompanying notes appear in Appendix~\ref{app:notes}.
