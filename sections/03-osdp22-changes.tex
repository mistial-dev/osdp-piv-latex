\section{Changes to OSDP 2.2}\label{sec:osdp22-changes}

This section documents the normative deltas that apply when an OSDP~2.2 implementation adopts the Enhanced PIV profile. Each subsection references the original command definition and records the precise behavioral updates, encoding clarifications, and new error handling rules introduced by this proposal.

\subsection{Updated Command Definitions}

\subsubsection{Authentication Challenge (osdp\_CRAUTH)}

OSDP~2.2 transports the ISO/IEC~7816-4 \code{GENERAL AUTHENTICATE} command through \texttt{osdp\_CRAUTH}. The OSDP~3.0 profile maintains the same framing fields, named \texttt{MpSizeTotal}, \texttt{MpOffset}, and \texttt{MpFragmentSize} as in the generic OSDP multi-part structure, but clarifies how the command payload maps to the PIV operation selected by the algorithm and key reference.

\begin{itemize}
  \item The \emph{Algorithm} field continues to be sent in the first fragment only. Codes \code{0x05}, \code{0x06}, and \code{0x07} select RSA private-key operations and preserve backwards compatibility with OSDP~2.2. Codes \code{0x08}, \code{0x0A}, and \code{0x0C} select AES encryption using 128-, 192-, and 256-bit keys respectively. Codes \code{0x11} and \code{0x14} identify ECC P-256 and P-384 mechanisms; the selected PIV key reference determines whether the mechanism is used for ECDSA signature generation or ECDH key establishment.
  \item The \emph{Key} field retains its role as the card’s reference identifier. PDs \textbf{SHALL NOT} reinterpret or remap the identifier.
  \item For RSA algorithms (\code{0x05}, \code{0x06}, \code{0x07}), the \emph{Challenge} field carries the encoded datum for the RSA private-key operation. The PD forwards the value unmodified; padding and digest encoding are the responsibility of the ACU.
  \item For AES algorithms (\code{0x08}, \code{0x0A}, \code{0x0C}), the \emph{Challenge} field contains the plaintext block(s) to be encrypted. PDs \textbf{SHALL} send the data to the credential without padding; ACUs \textbf{SHALL} provide data aligned to the block size dictated by the selected key length and card profile.
  \item For ECC signature operations using key references \code{0x9A}, \code{0x9C}, or \code{0x9E} with algorithms \code{0x11} or \code{0x14}, the \emph{Challenge} field carries the pre-hashed input to be signed. The PD \textbf{SHALL} issue \code{GENERAL AUTHENTICATE} with command data encoded as \texttt{7C Len 82 00 81 Len Challenge}; the credential returns the ECDSA signature in tag \code{0x82}. Exponentiation tag \code{0x85} \textbf{SHALL NOT} be used with \code{0x9A}, \code{0x9C}, or \code{0x9E}.
  \item For ECC key-establishment operations using key reference \code{0x9D} or retired key-management references, algorithms \code{0x11} and \code{0x14} use the \emph{Challenge} field to convey the peer public key. The PD \textbf{SHALL} issue \code{GENERAL AUTHENTICATE} with command data encoded as \texttt{7C Len 82 00 85 Len (04 || X || Y)}, where \texttt{X} and \texttt{Y} are the uncompressed coordinates for the selected curve and are left-padded with \code{0x00} as required. The credential performs elliptic-curve Diffie-Hellman in accordance with SP~800-73.
\end{itemize}

\input{tables/osdp-crauth}

\paragraph{Deprecated osdp\_GENAUTH} The \texttt{osdp\_GENAUTH} command is deprecated and expected to be removed in a future revision of this specification. Using a PIV credential to decrypt or process arbitrary data is unsafe and unnecessary for credential verification; implementations \textbf{SHOULD} use \texttt{osdp\_CRAUTH} flows instead.

\subsubsection{Response to Challenge (osdp\_CRAUTHR)}

The reply continues to use the OSDP~2.2 multi-part format. Successful execution yields a response containing the credential’s entire \code{GENERAL AUTHENTICATE} data (minus the ISO/IEC~7816 status word), including the returned Dynamic Authentication Template; PDs \textbf{SHALL NOT} strip outer BER-TLV tags or lengths. If the credential reports an error, the PD \textbf{SHALL} return the unchanged \texttt{osdp\_CRAUTHR} reply (for example with \texttt{MpFragmentSize = 0}) to preserve OSDP~2.2 behaviour. When available, the PD \textbf{SHOULD} also return \texttt{osdp\_PDERROR} on a subsequent \texttt{osdp\_POLL} with an applicable PIV code—for example \code{0x1020} (function not supported by credential), \code{0x1022} (insufficient credential storage), \code{0x1023} (credential security status not satisfied, including PIN-required conditions), \code{0x1024} (data object/tag not found), \code{0x1025} (VCI not established), or \code{0x1027} (credential not present/removed). Generic parameter errors may continue to use \code{0x0005} (incorrect parameter). PIN-entry outcomes are reported through command-specific result paths, such as \texttt{osdp\_PIVSPER}, when secure PIN entry is used. ACUs \textbf{SHALL} tolerate an optional \texttt{osdp\_PDERROR} following that reply; OSDP~2.2 ACUs may log and ignore the unknown reply code.

\paragraph{Reply structure}\mbox{}\\
\texttt{osdp\_CRAUTHR} \textbf{SHALL} return the credential’s full ISO/IEC~7816
\code{GENERAL AUTHENTICATE} response payload, including the Dynamic
Authentication Template, framed by the standard OSDP multi-part fields. PDs
\textbf{SHALL NOT} include SW1\textbar SW2 in \texttt{osdp\_CRAUTHR}; any
status words from the credential \textbf{SHALL} be carried only in
\texttt{osdp\_PDERROR} when a card/APDU failure occurs. PDs \textbf{SHALL NOT}
strip outer BER-TLV tags or lengths from the returned payload.

\input{tables/osdp-crauthr}

\subsubsection{Get PIV Data (osdp\_PIVDATA)}

OSDP~2.2 defined \texttt{osdp\_PIVDATA} with a five-byte payload that selected a PIV object and optional element. The original text did not explicitly specify how to pad identifiers shorter than three bytes, how nested tags should be returned, or how to interpret the data offset. OSDP~3.0 clarifies these behaviors so that ACUs and PDs apply a single normative approach.

\begin{itemize}
  \item The ACU \textbf{SHALL} transmit the PIV Object Identifier as three bytes encoded most-significant byte first. When the identifier contains fewer than three significant bytes, the ACU \textbf{SHALL} left-pad the value with \code{0x00} or \code{0x0000}, as required, so that the transmitted field is exactly three bytes. The PD \textbf{SHALL NOT} forward those pad bytes to the credential; it \textbf{SHALL} issue the ISO~7816-4 \texttt{GET DATA} APDU using only the significant identifier bytes. If VCI secure messaging is established, the PD \textbf{SHALL} use the appropriate secure channel automatically; no explicit selector is present in the command.
  \item The field previously described as “PIV element ID” is renamed \emph{PIV Tag}. A tag value of \code{0x00} directs the PD to return the complete BER-TLV structure, including the native outer tag (for example \code{0x7E} Discovery, \code{0x7F61} BIT Group Template) or the \code{0x53}/\code{0x73} wrapper used by other PIV objects. Any non-zero value selects a single root-level tag within the object. When the outer tag is \code{0x53} or \code{0x73}, the PD \textbf{SHALL} strip that wrapper before returning the selected tag. For other outer tags, the PD \textbf{SHALL} return the requested tag if present and \textbf{SHALL NOT} reject the request solely because the wrapper is not \code{0x53}/\code{0x73}. Tags \textbf{SHALL} be a single byte in the range \code{0x01}-\code{0xFF}; multi-byte DER tags are not supported and \textbf{MUST} be retrieved by issuing a tag value of \code{0x00} and parsing the returned object at the ACU.
  \item The data offset now supports one- or two-byte encodings. PDs \textbf{SHALL} accept either form and determine which was used based on the command length. The offset applies to the selected payload (entire object when the tag is zero, or the chosen tag otherwise). When a credential-side error occurs, PDs \textbf{SHALL} return the unchanged \texttt{osdp\_PIVDATAR} reply (for example with \texttt{MpFragmentSize = 0}) to maintain OSDP~2.2 compatibility. If available, PDs \textbf{MAY} report additional detail via \texttt{osdp\_PDERROR} on a subsequent \texttt{osdp\_POLL} using code \code{0x1023} when security prerequisites are not satisfied, \code{0x1024} when the requested object or tag is absent, or \code{0x1027} when the credential is not present or is removed; see Subsection~\ref{sec:new-piv-error-codes}. ACUs \textbf{SHALL} tolerate an optional \texttt{osdp\_PDERROR} following that reply; OSDP~2.2 ACUs may log and ignore the unknown reply code.
\end{itemize}

\input{tables/osdp-pivdata}

\subsubsection{PIV Data Reply (osdp\_PIVDATAR)}

The reply structure is unchanged from a field-count perspective, but OSDP~3.0 aligns the semantics with the command refinements above:

\begin{itemize}
  \item \texttt{MpSizeTotal}, \texttt{MpOffset}, and \texttt{MpFragmentSize} continue to be encoded LSB first; they now explicitly report the size, fragment base, and fragment length for the content selected by the \texttt{osdp\_PIVDATA} request (entire object or chosen tag).
  \item \texttt{CARD\_DATA} \textbf{SHALL} convey the exact bytes obtained from the credential after applying any tag stripping specified by the command. No additional framing or padding is permitted.
\end{itemize}

\input{tables/osdp-pivdatar}

\subsection{PD Error Codes for PIV}\label{sec:new-piv-error-codes}

PD error codes are carried in \texttt{osdp\_PDERROR} using two-byte codes (least-significant byte first). The common PD codes remain available (for example \code{0x0001} SC\_ERROR with optional SW1\textbar SW2, \code{0x0002} SC\_TIMEOUT, \code{0x0004} TLV\_MISSING, \code{0x0005} INVALID\_PARAM, \code{0x0006} SECURE\_CHANNEL\_REQUIRED, \code{0x0007} MP\_INVALID, \code{0x0008} MP\_OUT\_OF\_SEQUENCE, \code{0x0009} MP\_OUT\_OF\_BOUNDS). The following PIV-specific codes occupy the \code{0x1000}-\code{0x10FF} range:

\begin{ValueDescriptionTable}{Enhanced PIV Error Codes}{Code}
  \ValueDescriptionRow{0x1020}{Function not supported by credential (PD capability gaps \textbf{SHALL} use \texttt{osdp\_NAK} function-not-supported).}
  \ValueDescriptionRow{0x1021}{Insufficient PD memory or storage to complete the requested operation.}
  \ValueDescriptionRow{0x1022}{Insufficient credential memory or storage space.}
  \ValueDescriptionRow{0x1023}{Credential security status not satisfied.}
  \ValueDescriptionRow{0x1024}{Data object or tag not found.}
  \ValueDescriptionRow{0x1025}{VCI not established.}
  \ValueDescriptionRow{0x1027}{Credential not present or removed.}
\end{ValueDescriptionTable}

\subsubsection{PD Application Error Reply (osdp\_PDERROR)}

PDs \textbf{SHALL} use \texttt{osdp\_PDERROR} only for credential-side or other global application errors that are not already defined in a command-specific reply. The error code is two bytes, encoded least-significant byte first. When a card/APDU failure occurs (for example SC\_ERROR or a PIV APDU failure), the PD \textbf{MAY} append the ISO/IEC~7816 status word (SW1\textbar SW2) when it is available to the application layer; otherwise the status word is omitted. A single error is reported per \texttt{osdp\_PDERROR} reply. Transport-level faults (framing, CRC, sequence) continue to use \texttt{osdp\_NAK}.

\input{tables/osdp-pderror}

\subsection{Updated Function Codes}

\subsubsection{Function Code 12 – Smart Card Support}

Function Code~12 advertises smart-card transport capabilities and profile support at the PD. The compliance byte uses the following bit assignments:

\input{tables/function-code-12}

Bits not listed are reserved and \textbf{SHALL} be transmitted as zero.

\subsubsection{Function Code 13 – Reader Interfaces}

OSDP~2.2 defined Function Code~13 as a reader count indicator with a compliance byte fixed at \code{0x00}. Enhanced PIV-capable PDs \textbf{SHALL} continue to populate the reader count field, but they MAY set bits within the compliance byte to advertise the downstream credential technologies they expose. A value of \code{0x00} remains valid and retains the original OSDP~2.2 meaning of “interface presence unspecified.”

When a PD reports specific interfaces, it \textbf{SHALL} encode the compliance byte using the flags in Table~\ref{tab:function-code-13-flags}. The reader count byte continues to report the number of attached credential interfaces.

\begin{ValueDescriptionTable}{Function Code 13 Compliance Flags}{Mask}
  \label{tab:function-code-13-flags}
  \ValueDescriptionRow{0x01}{PD presents a contact card interface (ISO~7816 or equivalent).}
  \ValueDescriptionRow{0x02}{PD presents a high-frequency contactless credential interface (13.56~MHz NFC).}
  \ValueDescriptionRow{0x04}{PD presents a low-frequency credential interface (approximately 125~kHz prox).}
  \ValueDescriptionRow{0x08}{PD supports Virtual Contact Interface (VCI) secure messaging for applicable modes.}
  \ValueDescriptionRow{0x10}{PD presents a Bluetooth credential interface.}
  \ValueDescriptionRow{0x20}{PD presents a barcode credential interface.}
  \ValueDescriptionRow{0x40}{PD presents an ultra-high-frequency credential interface (UHF/RAIN RFID).}
\end{ValueDescriptionTable}
\noindent All other bits are reserved for future use and \textbf{SHALL} be transmitted as zero.

\subsubsection{Function Code 14 – Biometric Support}

Function Code~14 remains backward compatible with OSDP~2.2. A compliance value of \code{0x00}, \code{0x01}, \code{0x02}, or \code{0x03} retains its original meaning and indicates that the PD does not support the OSDP~3.0 biometric command set. Devices that support the enhanced commands \textbf{SHALL} set Bit~7 (\code{0x80}). When Bit~7 is asserted, the remaining bits of the compliance byte are interpreted as a capability bitfield. Bits not explicitly defined in Table~\ref{tab:function-code-14-flags} are reserved and \textbf{SHALL} be transmitted as zero.

\input{tables/function-code-14}
