\begin{ValueDescriptionTable}{SPE Flag Values}{Mask}
  \label{tab:pivspe-flags}
  \ValueDescriptionRow{0x02}{Require an explicit cache-clear command; do not auto-clear cached PINs on credential removal.}
  \ValueDescriptionRow{0x04}{Skip pairing-code verification when the credential and policy permit VCI without a pairing code.}
  \ValueDescriptionRow{0x08}{Auto-execute VCI verification when the credential is presented after a successful pre-cache (Mode \code{0x02}).}
  \ValueDescriptionRow{0x01, 0x10-0x80}{Reserved and \textbf{SHALL} be transmitted as zero.}
\end{ValueDescriptionTable}
